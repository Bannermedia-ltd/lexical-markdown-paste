# Changelog

## 0.1.0 — Unreleased

Initial release.

- `MarkdownPastePlugin` with auto and prompt modes
- Heuristic markdown detection with configurable threshold
- `getMarkdownScore` and `isLikelyMarkdown` utilities
- Safe paste behaviour (empty editor only)
